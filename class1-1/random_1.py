# random.py

def randint(arg_a, arg_b):
    print("흐흐흫")
    
test_1 = "random.py"
P = "라라라라"

# 함수가 나온 이유
# 전체 알고리즘을 모듈화 하기 위해서 나옴

# 하나의 모듈은 하나의 .py와 맵핑이된다.
# 상식 ! 번역기에는 컴파일러와 인터프리터가 있는데,
# 인터프리터로 번역되는 코드들은 scriptfile (.py) 이라고 한다.
# 스크립트파일의 이름이 모듈의 이름이 된다.

# 모듈은 함수, 클래스, 변수 등을 포함하는 단일 스크립트 파일
# 패키지는 여러 모듈을 포함하는 디렉토리 -> 복잡한 프로젝트를 체계적을 관리할 수 있게 해준다.
